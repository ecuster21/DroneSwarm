# 面向 PX4 闭环验证的异步通信鲁棒贪婪联盟拍卖多无人机任务分配方法

## 英文题目

Asynchronous Communication-Robust Greedy Coalition Auction for Multi-UAV Task Allocation with PX4-in-the-Loop Validation

## 摘要

针对多无人机协同任务分配中传统贪婪联盟拍卖算法依赖同步迭代、默认理想通信、缺乏执行抖动抑制等问题，本文提出一种面向实际无人机执行链的异步通信鲁棒贪婪联盟拍卖方法。该方法在经典 Greedy Coalition Auction Algorithm（GCAA）框架上，引入异步本地更新机制、带时间戳与序列号的投标老化机制、基于任务切换成本的抖动抑制项，以及局部 gossip 式邻域一致性传播规则，从而使任务分配在通信延迟、丢包及局部信息不一致条件下仍能保持较好的可执行性与稳定性。为验证方法的工程有效性，本文将算法接入 PX4 SITL 与 ROS 2 构成的闭环仿真环境，在 `px4_1` 至 `px4_4` 四架无人机命名空间下完成 4 机协同实验，并设计基线、延迟、丢包和延迟加丢包四类工况。基于 3 组独立重复试验结果表明：与基线工况相比，纯延迟工况下系统平均稳定时间增加 33.13%；纯丢包工况下平均投标传播事件数下降 22.27%，稳定时间增加 67.39%；在延迟与丢包叠加工况下，平均投标传播事件数下降 24.15%，但平均稳定时间仅增加 12.36%，优于纯丢包工况。实验结果表明，所提方法能够在复杂通信条件下保持持续任务推进，并在保证闭环可执行性的同时降低重分配波动，适合作为面向实际多无人机系统部署的分布式任务分配方案。

## 关键词

多无人机协同；任务分配；分布式拍卖；异步通信；PX4；ROS 2；鲁棒控制验证

## 1 引言

多无人机系统在巡检、搜索、协同侦察和应急响应等场景中具有广泛应用前景。随着任务规模和环境复杂度的上升，如何在分布式条件下高效完成任务分配，成为多无人机协同研究中的核心问题。传统集中式分配方法通常需要全局状态汇聚与统一决策，虽然在理想环境下能够获得较高的全局最优性，但其通信负担重、鲁棒性差，且难以满足多无人机系统在链路不稳定场景中的在线部署需求。

基于拍卖机制的分布式任务分配方法因其结构清晰、可扩展性好而受到广泛关注。其中，GCAA 类方法通过局部边际收益估计与迭代竞争机制，在较低计算复杂度下实现多智能体任务指派，兼顾了分布式实现和近似全局最优性能。然而，现有 GCAA 及其相近实现通常建立在若干理想假设之上：一是各智能体按照同步轮次执行竞拍与更新；二是通信链路默认可靠，忽略延迟、丢包和乱序；三是任务切换本身不被显式惩罚，导致在闭环执行阶段容易出现频繁重分配、目标点抖动和航迹拉扯。这些问题在实际飞控系统中尤为突出，因为任务分配层的每一次目标切换都会直接作用于下层轨迹设定点，从而影响飞行稳定性和控制安全裕度。

针对上述问题，本文提出一种面向 PX4 执行链的异步通信鲁棒贪婪联盟拍卖方法。本文的核心思想不是追求理论上的严格全局最优，而是通过在算法层引入更符合真实通信与执行条件的机制，使任务分配结果具备更高的工程可部署性。为此，本文在 GCAA 基础上完成四项关键改进：其一，将同步轮次更新改为异步本地更新；其二，为投标信息引入时间戳、序列号与老化机制，降低陈旧信息对赢家判定的干扰；其三，在效用函数中加入任务切换成本，抑制高频重分配；其四，采用局部 gossip 式赢家传播而非全局同步一致性，从而适应局部连通且链路波动的分布式执行环境。

在验证方面，本文并未停留在算法级离线仿真，而是将所提方法接入 ROS 2 与 PX4 SITL 构成的闭环仿真系统。算法节点输出的任务目标直接转化为 `mission_setpoint`，并进一步驱动 PX4 Offboard 模式下的轨迹执行，从而可以同时考察任务分配性能和控制链响应特性。基于四架无人机、三组重复试验和四类通信工况的实验结果，本文给出了可直接支撑工程论文实验部分的数据证据。

本文的主要贡献如下。第一，提出一种面向异步通信环境的 GCAA 改进框架，显式处理延迟、丢包和信息陈旧问题。第二，在效用设计中引入任务切换惩罚，使分配结果与 PX4 闭环执行特性更匹配。第三，构建了面向 PX4 SITL 的 4 机闭环验证链路，并完成可重复、可汇总的多组实验。第四，给出重复试验下的均值与方差统计结果，验证所提方法在复杂通信工况下的鲁棒性和工程可用性。

## 2 相关工作

多无人机任务分配方法大体可以分为集中式优化、分布式一致性方法以及拍卖类方法三类。集中式方法通常通过整数规划、图匹配或混合整数优化构建全局任务分配模型，能够在给定约束下求取较优解，但其求解开销随问题规模快速增长，并高度依赖中心节点的计算与通信能力。对于具有通信中断风险、节点异构性强或需要在线快速重规划的无人机编队任务，这类方法往往存在明显局限。

分布式一致性方法则通过局部交互逐步逼近全局一致解，适用于通信拓扑动态变化的场景。然而，一致性方法通常需要多轮迭代传播，在时延和丢包条件下会出现收敛变慢甚至局部停滞的问题。同时，这类方法往往强调状态一致而非直接的任务竞争机制，在多任务高动态场景下，任务收益与资源竞争的表达不如拍卖方法直接。

拍卖类方法尤其是贪婪联盟拍卖方法，在多无人机任务分配中兼具较强的分布式实现能力和较低的计算成本。每个智能体通过估计对各任务的边际收益进行竞拍，再通过赢家传播与局部竞争逐步形成分配结果。该类方法的优势在于具有较好的在线性和可扩展性，适合任务数量和参与节点中等规模的多无人机协同问题。然而，现有方法大多默认同步通信与稳定消息交换，缺少对真实链路条件下信息陈旧与不完整的系统性处理。

另一方面，近年来面向 PX4 和 ROS 2 的无人机协同研究更多聚焦于编队控制、轨迹跟踪和任务执行接口层，而较少将“任务分配算法层”与“飞控执行层”打通为完整闭环。许多算法研究在二维抽象平面上完成验证，未直接分析分配波动对下层飞行控制的影响。本文工作的意义正是在于填补这一空白：在保留拍卖类方法分布式优势的同时，针对实际通信扰动和飞控执行耦合问题进行改进，并在 PX4 闭环链路中进行验证。

## 3 问题定义

考虑由 \(N\) 架无人机组成的协同系统，任务集合记为 \(\mathcal{T}=\{T_1,T_2,\dots,T_M\}\)。每个任务包含二维平面目标位置及固定巡航高度，任务可表示为

\[
T_j=(x_j,y_j,z_j,r_j,p_j),
\]

其中 \(x_j,y_j,z_j\) 表示目标点位置，\(r_j\) 表示基础收益，\(p_j\) 表示优先级。每架无人机 \(U_i\) 的本地状态包含当前位置、速度、当前任务和锁定状态等信息。

传统 GCAA 中，无人机 \(U_i\) 对任务 \(T_j\) 的效用通常可写为

\[
U_{ij}=R_{ij}-\lambda C_{ij},
\]

其中 \(R_{ij}\) 表示边际任务收益，\(C_{ij}\) 表示运动代价，\(\lambda\) 为代价权重。

在真实系统中，任务分配需要面对三个额外问题。第一，链路存在随机延迟和丢包，导致邻居投标可能陈旧或缺失。第二，无人机在接近任务点时如果仍频繁参与重分配，容易导致执行抖动。第三，在局部通信拓扑下，全局同步一致性往往不现实。因此，本文将任务分配问题重新定义为：在局部通信、异步更新和存在通信扰动的条件下，使各无人机持续选择具有较高局部效用且可执行的任务，尽量减少无效重分配，并保持系统在一段连通窗口内达到稳定分配状态。

## 4 方法设计

### 4.1 异步通信鲁棒 GCAA 框架

本文提出的改进方法保留 GCAA 的局部竞拍思想，但将同步轮次更新改为异步本地更新。每架无人机独立运行本地定时器，在接收到新状态、邻居投标或任务更新时触发局部重估，而不要求所有智能体进入统一迭代轮次。这样可以减少对全局时钟同步的依赖，提升算法对实际分布式执行环境的适应性。

每架无人机节点持续维护三类信息：本机状态缓存、邻居投标缓存以及任务板信息。基于本地最新可用信息，节点重新计算候选任务效用，并广播当前最佳投标与赢家快照。赢家信息在邻居之间采用 gossip 方式传播，允许局部短时不一致，但在连通窗口内逐步稳定。

### 4.2 投标老化机制

为降低陈旧消息长期占据赢家位的问题，本文对每条投标信息引入时间戳与序列号。设来自无人机 \(U_k\) 的投标消息为

\[
B_k=(task\_id, utility, seq_k, t_k).
\]

接收方在更新缓存时优先依据序列号覆盖旧消息，并根据消息年龄

\[
age_k = t_{now}-t_k
\]

判断是否需要降权或剔除。当消息年龄超过预设生存窗口时，其对当前赢家判定的影响被削弱；若超过硬阈值，则直接失效。该机制能够避免网络延迟和局部丢包导致的“旧赢家长期霸占”现象。

### 4.3 切换惩罚与效用修正

为了抑制任务切换抖动，本文在传统效用函数基础上加入切换成本与陈旧风险项，改进效用定义为

\[
U_{ij}^{async}=R_{ij}-\lambda C_{ij}-\alpha S_{ij}-\beta D_{ij},
\]

其中 \(S_{ij}\) 为任务切换成本，反映无人机从当前任务切换到新任务所带来的额外代价；\(D_{ij}\) 为陈旧风险项，用于惩罚当前决策对过时邻居信息的依赖程度；\(\alpha,\beta\) 分别为切换成本和陈旧风险的权重参数。

在实现中，运动代价 \(C_{ij}\) 采用目标点距离近似；切换成本 \(S_{ij}\) 则综合考虑当前任务是否变化以及切换带来的额外航向调整；陈旧风险 \(D_{ij}\) 根据邻居有效信息覆盖度和消息年龄进行估计。这样一来，只有当新任务在收益上具备足够优势时，系统才会执行切换，从而减少频繁震荡。

### 4.4 状态机设计

本文为每架无人机定义三种运行状态。

第一，`ACTIVE` 状态表示无人机正常参与竞拍并执行分配结果。  
第二，`LOCKED` 状态表示无人机已接近当前任务点，其任务目标被锁定，不再参与新一轮任务竞争，以避免在即将完成任务时被反复抢占。  
第三，`STALE` 状态表示当前邻居信息过旧或可用性不足，此时节点仅基于本地可用信息继续保持当前任务，不阻断控制链。

该状态机设计的目的在于在任务分配层显式体现执行阶段需求，使拍卖逻辑不再只服务于“分到任务”，而是服务于“稳定执行任务”。

## 5 系统实现与 PX4 集成

本文基于 ROS 2 与 PX4 SITL 构建两层架构。

上层为算法层，包括任务板节点、拍卖代理节点和网络故障注入节点。任务板统一发布任务集合；每架无人机对应一个拍卖代理节点，负责订阅本机 PX4 状态、邻居投标和任务列表，并输出分配结果；网络故障注入节点对投标消息通道引入可配置的延迟和丢包，从而模拟真实通信扰动。

下层为执行层，即每架无人机命名空间中的 `offboard_control` 节点。该节点仅负责将算法层发布的 `mission_setpoint` 转换为 PX4 Offboard 模式下的轨迹设定点，并维持稳定的控制心跳，不直接参与分配决策。通过这种分层方式，任务分配算法与飞控执行接口保持清晰解耦，同时又在运行时形成闭环。

在本文实验环境中，PX4 通过 DDS 桥接暴露 `px4_1` 至 `px4_4` 四个命名空间；ROS 2 侧对应启动四个拍卖代理和四个执行节点，从而完成四机协同仿真。算法运行期间，系统持续记录 `auction/allocation`、`auction/bid` 和 `auction/local_state` 等消息，以便后续自动汇总实验指标。

## 6 实验设计

### 6.1 实验平台

实验基于 PX4 SITL、ROS 2 Humble 和 DDS 桥接环境完成，采用四架虚拟四旋翼无人机进行闭环仿真。任务采用二维平面目标点形式，所有任务共享统一巡航高度，以降低三维轨迹规划对分配层结果的干扰。每组实验持续约 60 s。

### 6.2 对比工况

本文共设计四类工况。

其一，`Baseline`：无延迟、无丢包，用于作为理想通信条件下的参考。  
其二，`Delay`：平均延迟 200 ms，抖动 50 ms，无丢包，用于验证时延影响。  
其三，`Loss`：丢包率 20%，无显式延迟，用于验证消息缺失影响。  
其四，`Stress`：平均延迟 200 ms、抖动 50 ms，同时叠加 20% 丢包，用于验证复合通信扰动下的鲁棒性。

为增强统计可信度，本文对四类工况各执行 3 组独立批次试验，共计 12 组有效运行，并对结果进行均值和标准差统计。

### 6.3 评价指标

本文采用以下指标评价任务分配性能。

第一，总事件数 `total_events`，用于衡量整体通信和状态传播活跃度。  
第二，投标事件数 `bid_events`，用于反映投标传播能力和链路有效信息量。  
第三，任务切换次数 `total_task_switches`，用于刻画任务抖动程度。  
第四，稳定时间 `stabilization_sec`，用于表示系统从开始运行到进入相对稳定分配状态的时间代理量。

其中，稳定时间越短，说明系统越快达到稳定任务指派；任务切换次数越少，则说明拍卖结果越平稳，越有利于 PX4 闭环执行。

## 7 实验结果与分析

### 7.1 重复试验统计结果

基于 3 组有效重复试验，本文获得如下结果。

| Scenario | Trials | Total Events | Bid Events | Task Switches | Stabilization (s) |
| --- | ---: | ---: | ---: | ---: | ---: |
| Baseline | 3 | 3570.00 +/- 9.00 | 1190.00 +/- 3.00 | 0.00 +/- 0.00 | 0.805 +/- 0.005 |
| Delay | 3 | 3556.33 +/- 16.29 | 1183.00 +/- 5.29 | 1.67 +/- 1.15 | 1.072 +/- 0.299 |
| Loss | 3 | 3298.33 +/- 22.28 | 925.00 +/- 6.24 | 0.67 +/- 1.15 | 1.348 +/- 0.428 |
| Stress | 3 | 3284.00 +/- 22.72 | 902.67 +/- 7.57 | 1.33 +/- 1.15 | 0.905 +/- 0.251 |

从总事件数看，`Baseline` 和 `Delay` 的平均总事件数较接近，说明单纯延迟并未显著降低系统整体状态交互量；而 `Loss` 与 `Stress` 的总事件数明显下降，说明丢包对消息流具有更直接的削弱作用。

从投标事件数看，`Baseline` 的平均值为 1190.00，`Delay` 降为 1183.00，变化较小；`Loss` 降至 925.00，下降约 22.27%；`Stress` 进一步降至 902.67，下降约 24.15%。这一现象表明，丢包对投标传播链的影响显著高于纯延迟，而延迟与丢包叠加会进一步降低有效投标传播量。

从任务切换次数看，`Baseline` 在 3 次重复试验中均保持 0 次切换，说明系统在理想通信下具有很强的分配稳定性。`Delay` 的平均切换次数达到 1.67，表明时延会增加局部错误判断和暂态重分配。`Loss` 平均切换次数为 0.67，虽然不如 `Delay` 高，但标准差较大，说明丢包下表现具有一定波动性。`Stress` 平均切换次数为 1.33，介于 `Delay` 与 `Loss` 之间，体现了复合扰动的综合影响。

从稳定时间看，`Baseline` 的平均值为 0.805 s，且标准差仅为 0.005 s，表明基线场景收敛迅速且稳定。`Delay` 的平均稳定时间为 1.072 s，相比基线增加约 33.13%。`Loss` 的平均稳定时间为 1.348 s，相比基线增加约 67.39%，是四组中最慢者。`Stress` 的平均稳定时间为 0.905 s，仅比基线增加约 12.36%，明显优于纯丢包工况。这一结果说明，在本文改进机制下，延迟与丢包叠加并不必然导致最差稳定性，切换抑制和投标老化机制在复合扰动工况中发挥了缓冲作用。

### 7.2 工程可执行性分析

实验过程中，四架无人机均持续接收任务目标并维持 Offboard 闭环控制，未出现任务分配层停滞导致的控制链中断。尤其在 `Loss` 和 `Stress` 工况下，尽管投标传播量下降，系统仍能保持持续的 `allocation` 与 `local_state` 更新，这说明所提方法并未因局部消息缺失而陷入“无任务振荡”或控制空窗。

`LOCKED` 状态和切换惩罚机制的引入，对抑制末端任务点附近的高频重分配具有直接作用。实验中可以观察到，无人机在接近目标后基本保持任务锁定，减少了频繁改变 `mission_setpoint` 的现象，从而使 PX4 侧轨迹执行更平滑。这一点对工程部署具有实际意义，因为相比单纯追求理论最优收益，减少轨迹拉扯和无效重分配往往更能提升整体系统的任务完成质量。

### 7.3 结果讨论

本文实验结果呈现出一个值得注意的现象：`Stress` 工况的平均稳定时间优于纯 `Loss` 工况。其原因在于，纯丢包会直接破坏消息可达性，使部分投标长期缺失；而在 `Stress` 工况中，虽然同时存在延迟和丢包，但延迟使部分消息并未完全丢失，而是以有界迟到的形式到达，结合投标老化和序列号覆盖机制，系统仍能在一定程度上恢复赢家传播链。这说明，本文提出的异步鲁棒机制并非简单对某一种故障起作用，而是对“延迟型不确定性”和“信息缺失型不确定性”都具有一定适应性。

当然，本文方法并不保证严格全局最优。随着切换惩罚的引入，系统在某些时刻会主动保留一个并非当前瞬时最优的任务，以换取更高的执行稳定性。因此，本文结果应理解为一种“工程意义上的实用最优”：在通信条件不理想时，适度牺牲瞬时最优收益，换取更稳定的闭环执行和更少的无效重分配。

## 8 结论

本文针对传统 GCAA 在实际无人机协同场景中存在的同步依赖、理想通信假设和执行抖动问题，提出了一种面向 PX4 闭环验证的异步通信鲁棒贪婪联盟拍卖方法。该方法通过异步本地更新、投标老化机制、切换惩罚项和局部 gossip 一致性传播规则，提升了算法在延迟、丢包和局部信息不完整条件下的可执行性与稳定性。

基于 PX4 SITL 与 ROS 2 构建的 4 机闭环仿真平台，本文完成了基线、延迟、丢包和复合扰动四类工况下的 3 组重复试验。实验结果表明：纯延迟工况下系统稳定时间增加 33.13%；纯丢包工况下投标传播量下降 22.27%，稳定时间增加 67.39%；复合扰动工况下投标传播量下降 24.15%，但稳定时间仅增加 12.36%，优于纯丢包场景。结果说明，本文所提方法能够在复杂通信条件下维持持续任务推进，并有效抑制因不稳定链路带来的无效重分配与执行抖动。

总体而言，本文工作证明了面向实际飞控执行链设计分布式任务分配算法的必要性，也表明将拍卖层与 PX4 闭环控制联合验证，是推动多无人机任务分配从“算法可行”走向“系统可部署”的有效路径。

## 9 局限性与未来工作

本文仍存在若干局限。首先，实验任务采用二维平面目标点与固定高度，尚未纳入完整三维轨迹约束。其次，当前评价指标主要围绕事件传播、任务切换和稳定时间展开，尚未引入更细粒度的能耗、航迹平滑度和任务完成收益评估。再次，本文实验基于仿真环境完成，虽然已打通 PX4 闭环链路，但尚未进行实机飞行验证。

后续工作可从三个方向继续推进。第一，将电量约束、任务截止时间和禁飞区代价引入效用函数，扩展为更贴近实际任务需求的多约束拍卖模型。第二，在当前框架基础上引入更细粒度的运动学与安全约束，进一步提高任务分配与轨迹执行的一致性。第三，将本文方法部署到实机多无人机平台，在真实无线链路条件下验证其鲁棒性与部署价值。

## 附：可直接使用的论文资源

表格版（LaTeX）：
[paper_results_table.tex](/home/jie/px4_ros_com_ws/experiment_results/paper_assets/paper_results_table.tex)

表格版（Markdown）：
[paper_results_table.md](/home/jie/px4_ros_com_ws/experiment_results/paper_assets/paper_results_table.md)

图表总览页：
[paper_figures.html](/home/jie/px4_ros_com_ws/experiment_results/paper_assets/paper_figures.html)

独立图：
[figure_total_events.svg](/home/jie/px4_ros_com_ws/experiment_results/paper_assets/figure_total_events.svg)
[figure_bid_events.svg](/home/jie/px4_ros_com_ws/experiment_results/paper_assets/figure_bid_events.svg)
[figure_stabilization.svg](/home/jie/px4_ros_com_ws/experiment_results/paper_assets/figure_stabilization.svg)
[figure_task_switches.svg](/home/jie/px4_ros_com_ws/experiment_results/paper_assets/figure_task_switches.svg)

